[package]
name = "wildnix"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.6.3"
x86_64 = { version = "0.15", default-features = false, features = ["instructions"] }

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"